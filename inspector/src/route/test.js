/**
 * Created by apletea on 9.6.17.
 */
let errorUtil = require('../utils/errors').errorCodes,
    responseUtil = require('../utils/response'),
    authHelper = require('../utils/auth');


module.exports = (express,passport) => {
    let router =  express.Router();


};